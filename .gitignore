# macOS
.DS_Store

# Xcode / Swift
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
.swiftpm/
Package.resolved

# XcodeGen 생성물 — 원본은 project.yml (`xcodegen generate`로 재생성)
*.xcodeproj

# 비밀/키 (API 키는 절대 커밋 금지 — B4 참조)
*.xcconfig.secret
Secrets.plist
.env

# Supabase CLI temp
supabase/.temp/
